server:
    port: 8081
    servlet:
        context-path: /identity/v1

spring:
    datasource:
        url: "jdbc:mysql://localhost:3306/identity"
        username: root
        password: root
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
